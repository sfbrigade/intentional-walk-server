version: '3'
services:
  server:
    command: bash -l -c "bin/init; nf start -j Procfile.dev"
    ports:
      - ${CLIENT_PORT:-3000}:5000
    volumes:
      - .:/app
      - /app/client/node_modules
